<%= form_for(@category) do |f| %>
    <%=  render 'shared/error_messages', :object => f.object %>
    <div class="FormField">
      <%= f.label(:name) %>
      <br/>
      <%= f.text_field(:name)%>
    </div>
    <div class="FormActions">
      <%= f.submit("Save") %>
    </div>
<% end %>